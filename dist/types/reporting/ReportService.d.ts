export declare function reportViolation({ directive, blockedUri, documentUrl, originalPolicy, referrer }: {
    directive: string;
    blockedUri: string;
    documentUrl: string;
    originalPolicy: string;
    referrer?: string;
}): void;
